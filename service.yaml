apiVersion: v1
kind: Service
metadata:
  name: flask-auth-service
spec:
  type: NodePort
  selector:
    app: flask-auth
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
    nodePort: 30001